# 🗺️ نقشه سه‌بعدی ایران

یک نقشه سه‌بعدی تعاملی از ایران با استفاده از Three.js و WebGL

## 📁 ساختار فایل‌ها

```
├── index.html              # فایل اصلی HTML (شامل SVG و اسکریپت‌ها)
├── css/
│   ├── style.css          # استایل‌های CSS (جدا شده از HTML)
│   ├── iranmap.css        # استایل‌های نقشه SVG
│   └── iranmap.min.css    # نسخه فشرده
├── js/
│   ├── config.js          # تنظیمات نقشه (MAP_CONFIG, رنگ‌ها، ...)
│   ├── iranmap.js         # اسکریپت‌های نقشه
│   ├── iranmap.min.js     # نسخه فشرده
│   └── jquery-2.1.0.min.js
└── README.md              # این فایل

```

## 🎨 فایل‌های جدا شده

### 1. `css/style.css`
تمام استایل‌های صفحه شامل:
- استایل‌های پایه (body, container)
- پنل اطلاعات (#info)
- دکمه‌های کنترل (.controls, .btn)
- پنل تنظیمات (#control-panel)
- اسلایدرها و دکمه‌ها

### 2. `js/config.js`
تمام تنظیمات قابل تغییر:
- `MAP_CONFIG`: تنظیمات استان‌ها، دریاها، bloom، دوربین، نورپردازی
- `BACKGROUND_COLORS`: رنگ‌های پس‌زمینه
- `regionGroups`: رنگ‌بندی مناطق
- `provinceNames`: نام فارسی استان‌ها

### 3. `index.html`
فایل اصلی شامل:
- Template SVG نقشه ایران
- اسکریپت‌های Three.js
- کدهای اصلی برنامه

## 🚀 نحوه استفاده

### روش 1: استفاده از فایل واحد (فعلی)
همه چیز در `index.html` است - فقط این فایل را باز کنید.

### روش 2: استفاده از فایل‌های جدا (پیشنهادی)
1. فایل‌های CSS و JS را به `index.html` لینک کنید:

```html
<head>
    ...
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    ...
    <script src="js/config.js"></script>
    <script src="js/main.js"></script>
</body>
```

2. فایل را در مرورگر باز کنید

## ⚙️ تنظیمات

### تغییر رنگ‌ها
در `js/config.js`:
```javascript
const regionGroups = {
    northwest: { color: 0xff6644, provinces: [...] },
    // ...
};
```

### تنظیم ارتفاع دیواره‌ها
```javascript
MAP_CONFIG.provinces.extrudeDepth = 35;  // استان‌ها
MAP_CONFIG.water.extrudeDepth = 20;      // دریاها
```

### تنظیم گلو (Bloom)
```javascript
MAP_CONFIG.bloom.strength = 0.6;    // قدرت
MAP_CONFIG.bloom.radius = 0.4;      // شعاع
MAP_CONFIG.bloom.threshold = 0.55;  // آستانه
```

## 🎛️ پنل کنترل

برای نمایش/مخفی کردن پنل کنترل:
```javascript
const SHOW_CONTROL_PANEL = true;  // یا false
```

### امکانات پنل:
- ✅ تنظیم ارتفاع دیواره‌ها (بدون رفرش)
- ✅ تغییر رنگ مناطق (real-time)
- ✅ تنظیم شفافیت و مواد
- ✅ کنترل نورپردازی
- ✅ تنظیم افکت Bloom
- ✅ کپی تنظیمات
- ✅ بازگشت به پیش‌فرض

## 🔧 توابع کلیدی

### `loadAndCreateProvinces()`
خواندن SVG از template و ساخت استان‌ها

### `createProvinceFromPath(id, pathData, color)`
ساخت یک استان از path SVG

### `createSeaFromPath(id, pathData)`
ساخت دریاها و دریاچه‌ها

### `highlightProvince(province)`
هایلایت کردن استان موقع هاور

### `applyConfigChanges(path)`
اعمال تغییرات تنظیمات به صورت زنده

## 📦 وابستگی‌ها

- Three.js r128
- OrbitControls
- EffectComposer (برای Bloom)
- Vazirmatn Font

## 🎨 ویژگی‌ها

- ✨ نمایش سه‌بعدی با افکت شیشه‌ای
- 🌈 رنگ‌بندی مناطق با نئون
- 💫 افکت Bloom برای گلو
- 🎯 هاور تعاملی روی استان‌ها
- ⚙️ پنل کنترل کامل
- 🔄 تغییرات real-time
- 📱 Responsive

## 📝 نکات

1. **ارتفاع دیواره‌ها**: با `scale.z` تغییر می‌کند (بدون رفرش)
2. **رنگ مناطق**: فوراً اعمال می‌شود
3. **تنظیمات**: قابل export و import
4. **SVG**: داخل `<template>` برای سرعت بیشتر

## 🐛 عیب‌یابی

### نقشه لود نمیشه
- مطمئن شوید `<template id="iran-map-svg">` وجود دارد
- Console را چک کنید (F12)

### رنگ‌ها تغییر نمی‌کنند
- برای رنگ دریاها نیاز به رفرش است
- رنگ استان‌ها فوری اعمال می‌شود

### پنل کنترل نمایش داده نمیشه
- `SHOW_CONTROL_PANEL = true` را چک کنید
- دکمه "⚙️ تنظیمات" را کلیک کنید

## 📄 لایسنس

این پروژه آزاد و قابل استفاده برای همه است.

---

ساخته شده با ❤️ برای ایران
